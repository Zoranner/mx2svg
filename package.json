{
  "name": "mx2svg",
  "version": "0.1.0",
  "description": "Draw.io / diagrams.net XML → SVG（全新架构；分阶段实现）",
  "type": "module",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "bun test",
    "render": "bun run ./src/cli.ts",
    "format": "biome format --write ./src package.json biome.json",
    "format:check": "biome format ./src package.json biome.json",
    "check": "biome check --write ./src package.json biome.json"
  },
  "dependencies": {
    "@chenglou/pretext": "^0.0.5",
    "@napi-rs/canvas": "^0.1.97",
    "fast-xml-parser": "^5.2.5",
    "pako": "^2.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.11",
    "@types/bun": "latest",
    "@types/pako": "^2.0.4"
  },
  "engines": {
    "node": ">=20"
  }
}
